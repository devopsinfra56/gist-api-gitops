apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gist-api.fullname" . }}-test-connection"
  labels:
    {{- include "gist-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gist-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
